# Run: node scripts/generate-password-hash.mjs "your-password"
# Then paste the output values here.
ACCESS_PASSWORD_HASH=
COOKIE_SECRET=

# Neon Postgres — auto-set by Vercel when you connect a Neon database
DATABASE_URL=
